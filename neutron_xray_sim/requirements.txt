# neutron_xray_sim — Python dependencies
#
# Install with:  pip install -r requirements.txt
#
# ASTRA Toolbox (GPU-accelerated tomographic projection + reconstruction)
# requires a CUDA-capable GPU and conda:
#   conda install -c astra-toolbox -c nvidia astra-toolbox
#
# All other packages are pip-installable.

numpy>=1.24
scipy>=1.10
scikit-image>=0.21
matplotlib>=3.7
scikit-learn>=1.3    # for GMM fitting (optional but recommended)

# Optional: GPU acceleration
# astra-toolbox          # conda install -c astra-toolbox astra-toolbox
